/*

for (int i = 0; i < numSamples; i++) {
        std::cout << samples[i];
    }

std::cout << " disp: " << complexDisplacement;
std::cout << " arg: " << complexArg;
std::cout << " frame: " << frame;
std::cout << " real: " << std::real(frame);
std::cout << " imag: " << std::imag(frame);

std::cout << " real: " << sumReal;
std::cout << " imag: " << sumImag;

(std::cos(complexArg) + j * std::sin(complexArg))

std::cout << std::endl << "frequencies" << std::endl;
for (double el : freq) {
    std::cout << el << " ";
}
std::cout << std::endl << "amplitudes" << std::endl;
for (double el : amp) {
    std::cout << el << " ";
}

*/